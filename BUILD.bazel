load("@io_bazel_rules_go//go:def.bzl", "gazelle", "go_prefix")

go_prefix("nova")

gazelle(
    name = "gazelle",
    args = [
        "-build_file_name",
        "BUILD,BUILD.bazel",
    ],
    build_tags = [
        "integration",
        "debug",
    ]
)
